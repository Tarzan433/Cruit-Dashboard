Update the "Create Job Post - Location" step (Step 3) to match the design shown and add improvements:

**STEP PROGRESS BAR:**
- Show 6 steps total: Type, Details, Location, Terms, Requirements, Review
- Steps 1 (Type) and 2 (Details) show a green filled circle with a white checkmark ✓ (already completed)
- Step 3 (Location) shows a green filled circle with the number "3" in white (current step)
- Steps 4, 5, 6 show empty gray circles with their numbers, connected by a gray dashed/thin line
- Below each circle, show the step label in small text (11px):
  - Completed steps: dark/black label
  - Current step: dark/black label  
  - Future steps: light gray label
- Connect all circles with a horizontal line: green line between completed steps, gray line for upcoming

**PAGE HEADER:**
- Back arrow link "← Job Posts" at the top left of the content card
- Below that: small uppercase muted label "YOU'RE CREATING"
- Below that: a small green square icon with a "+" symbol, followed by bold large text "New Job Post"

**LOCATION FORM SECTION:**
- Section heading: "Location" in bold ~22px
- Two fields side by side in a row:
  - Left field: label "COUNTRY" (uppercase, small, muted), input with placeholder "Italy"
  - Right field: label "CITY *" (uppercase, small, muted, asterisk in red), input with placeholder "Milan"
  - Both fields: white background, light gray border, border-radius 8px, height 44px
- Below the two fields: full-width field:
  - Label "ADDRESS" (uppercase, small, muted)
  - Input with placeholder "Via Roma 1"
  - Same styling as above fields

**IMPROVEMENTS TO ADD:**
- Add a "State / Region" field between the two-column row and Address (optional field, full width)
- Add a "Work Type" toggle/selector below Address with 3 options: Remote | Hybrid | On-site
  (pill-style toggle, green for selected, gray border for unselected)
- Add subtle helper text below City: "Enter the city where the job is based"

**BUTTONS:**
- Bottom left: "Back" button — white background, gray border, border-radius 999px, padding 12px 28px
- Bottom right: "Continue" button — green background (#22c55e), white bold text, border-radius 999px, 
  padding 12px 60px, wider than Back button

**LAYOUT:**
- Entire form is in a centered white card, max-width 400px, centered on the page
- Card has no heavy shadow — subtle border or light shadow only
- Page background: light gray (#f0f2f5)
- All form labels: uppercase, font-size 11px, font-weight 600, color #6b7280, margin-bottom 4px
- Input focus state: green border (#22c55e) with subtle green glow

**Do NOT change:**
- The top navbar (Home, Search jobs, Job Posts)
- The green brand color (#22c55e) used throughout