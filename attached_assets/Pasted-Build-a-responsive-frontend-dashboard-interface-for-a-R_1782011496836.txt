Build a responsive frontend dashboard interface for a "Recruiter" user view based on the provided wireframe image, ensuring compatibility with VS Code and future Firebase integration.

Tech Stack Constraints:
- Use only HTML5, vanilla CSS3, and standard vanilla JavaScript (ES6+).
- Do not use Tailwind, React, or heavy UI frameworks. All components must be built natively to maintain a lightweight codebase. Standard SVG icons or simple CDN links (like FontAwesome) for standard UI icons (e.g., house, bell, envelope) are acceptable.

Layout & Structure to Build:

1. Top Navigation Bar (Recruiter State):
   - Left side: Logo brand "DAILYCRUIT" with a small icon.
   - Center: Nav links container featuring "Home", "Search jobs", and "Job Posts" (with a corporate office building icon). The "Home" link should show an active state (e.g., green text and icon, light background pill styling). Note: The old "My Applications" button is hidden.
   - Right side: Standard circular icon buttons for messages, notifications, and the Profile Avatar "T" with a green active status dot.

2. Main Dashboard Content Grid:
   - A centered multi-column layout on a light gray background containing the following card components:
   - Card 1: Welcome message: "Welcome back, Tarzan! 👋", sub-label "Here’s an overview of your activity", and a prominent solid green action button "+ New Post" with white text.
   - Card 2: Metrics block labeled "Total Views". Shows a green "eye" icon, a large large green value "0", and sub-label text "Across all posts".
   - Card 3: Metrics block labeled "Total Posts". Shows a green corporate document icon, a large large green value "0", and detailed sub-labels: "0 Active", "0 Draft", "0 Archived".
   - Card 4: Metrics block labeled "Applications". Shows a green stacked box icon, a large green value "0", and sub-label text "Recent received".

   - Row below metric blocks (Two column layout):
     - Left Column Card ("Recent Applications"): Card title "Recent Applications" on the left, "See all ->" link in green on the right. Below the title, a centered empty-state container with a large muted envelope icon and text "No applications yet".
     - Right Column Card ("Top Posts"): Card title "Top Posts" on the left, "See all ->" link in green on the right. Below the title, a centered empty-state container with a large muted corporate building icon and text "No posts yet".

   - Row below two-column layout (Left-aligned wide column):
     - "Recent Chats" Card: Card title "Recent Chats" on the left, "See all ->" link in green on the right. Below the title, arrange three stacked chat rows. Each row features a circular avatar with "M" initials, user name "Muhammed Faris", sub-label "No messages yet", and a gray right-pointing arrow icon.

Specific Interaction Logic (JavaScript):
- Modal Trigger: Write the minimal JavaScript needed to trigger a complete DOM update when the user clicks the "Change" button in the Settings, selecting "Recruiter".
- View Swapping: Implement logic that swaps out the entire underlying `jobSeekerDashboard` HTML container and injects the `recruiterDashboard` HTML content generated by this prompt.
- Modal Close & Settings Update: Add functionality to automatically update the Settings page "ACCOUNT TYPE" badge to "Recruiter" and close the "Change account type" modal dialog overlay after the selection is complete. Ensure this JS state change is clean for future Firebase database integration.

Please generate the modular HTML, a dedicated stylesheet isolating the recruiter dashboard layout and button classes, and the vanilla JS logic required to execute the dashboard swap and update the nav bar layout.